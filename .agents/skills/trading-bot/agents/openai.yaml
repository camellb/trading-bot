interface:
  display_name: "Trading Bot"
  short_description: "Repo-specific patterns and workflows for trading-bot"
  default_prompt: "Use the trading-bot repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true