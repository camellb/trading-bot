# Full Data Reset — 2026-04-21

**Reason:** The bot was operating under a broken paradigm (edge-hunting, Kelly sizing, shadow-mode risk brakes disabled, autonomous config tuning on tiny samples). All accumulated trades, evaluations, calibration data, and backtest results were generated under those flawed assumptions. The `CLAUDE.md` doctrine alignment made them unrepresentative of the current system; keeping them would poison calibration, backtests, and self-improvement suggestions.

**Scope:** Purge all runtime row data. Preserve schemas, `user_config` (reset to doctrine defaults), and the paused `bot_controls` row.

**Backup:** `db_backup_pre_reset_20260421_154526.sql` — 1.9 GB, verified clean footer. Gitignored via `db_backup_*.sql`.

**Rollback:** `psql "$DATABASE_URL" < db_backup_pre_reset_20260421_154526.sql`

---

## Steps Executed

1. **Inventory** — 27 tables enumerated. 25 runtime tables marked for purge; `user_config` and `bot_controls` marked keep-with-reset; `alembic_version` unchanged.
2. **Backup** — `pg_dump --no-owner --no-privileges` produced 18.9M-line SQL dump (1.9 GB, clean `-- PostgreSQL database dump complete` footer). `.gitignore` updated with `db_backup_*.sql`.
3. **Pause verification** — `bot_controls.global_pause = TRUE` (reason: "DIAGNOSTIC HALT — investigating 18% win rate inversion on 34 settled trades"). 0 `event_log` events in trailing 2 min. Running `main.py` daemon (PID 99719, respawned via launchd `com.tradingbot`) was stopped via `launchctl bootout` of both `com.tradingbot` and `com.tradingbot.watchdog`. Dashboard launch agent left running.
4. **TRUNCATE** — 25 tables truncated with `RESTART IDENTITY CASCADE` in a single transaction.
5. **user_config reset** — updated to doctrine defaults (idempotent — row already matched).
6. **Filesystem cleanup** — deleted `logs/sweep.log`, `logs/watchdog.log`, `_analysis_scripts/` (trade_analysis.py + pyc), `_archive/` (backtester pyc tree + .DS_Store).
7. **Verification** — all 25 runtime tables report 0 rows; keep tables intact; backup file present (1.9 GB).
8. **Paused-ready** — `global_pause = TRUE` retained. User unpauses manually when ready.
9. **Commit** — this audit doc + `.gitignore` change.

---

## Pre-purge row counts (destroyed)

| Table                     | Rows |
|---------------------------|------|
| backtest_runs             | 89 |
| backtest_signals          | 18,178,172 |
| backtest_trades           | 781,067 |
| calibration_snapshots     | 153 |
| config_change_history     | 0 |
| daily_pnl                 | 1 |
| event_log                 | 256 |
| feed_health_log           | 442 |
| macro_context_log         | 11 |
| market_blocklist          | 0 |
| market_evaluations        | 289 |
| market_watchlist          | 0 |
| markouts                  | 1,450 |
| news_event_log            | 3,559 |
| pending_suggestions       | 0 |
| performance_snapshots     | 0 |
| pm_positions              | 51 |
| positions                 | 0 |
| predictions               | 401 |
| priority_markets          | 0 |
| reconciliation_log        | 0 |
| sentiment_scores          | 1 |
| strategy_performance      | 2 |
| ticks                     | 40 |
| trades                    | 1 |
| **Total**                 | **18,965,985** |

## Post-purge row counts (verified)

All 25 tables = 0.

## user_config doctrine defaults (applied)

| Field                    | Value |
|--------------------------|-------|
| min_ev_threshold         | 0.03 |
| base_stake_pct           | 0.02 |
| max_stake_pct            | 0.05 |
| daily_loss_limit_pct     | 0.10 |
| weekly_loss_limit_pct    | 0.20 |
| drawdown_halt_pct        | 0.40 |
| streak_cooldown_losses   | 3 |
| dry_powder_reserve_pct   | 0.20 |

## bot_controls (retained)

| Field         | Value |
|---------------|-------|
| control_type  | global_pause |
| enabled       | TRUE |
| reason        | DIAGNOSTIC HALT — investigating 18% win rate inversion on 34 settled trades |

---

## Next actions (owned by user)

1. Manually flip `global_pause` to FALSE when ready to resume trading.
2. Restart the stopped launch agents if a clean restart is wanted:
   `launchctl bootstrap gui/$UID ~/Library/LaunchAgents/com.tradingbot.plist`
   `launchctl bootstrap gui/$UID ~/Library/LaunchAgents/com.tradingbot.watchdog.plist`
3. Monitor first 50 settled trades for calibration and EV realisation under the new doctrine-aligned system.
